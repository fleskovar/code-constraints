rules:
  - id: freeze-architectural-tags
    type: frozen-rules
    severity: error
    scope: diff
    classes: ["orders.**"]
    message: |
      Architectural tag drift: '{rule}' on '{member}' of '{qualified_name}'
      was {action}.
