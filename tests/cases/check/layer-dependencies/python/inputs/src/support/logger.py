class Logger:
    """Deliberately untagged. A reference *to* an untagged class is ignored -
    the hole in the matrix the catalogue warns about."""

    def write(self, line: str) -> None:
        pass
